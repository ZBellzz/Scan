$(document).ready(function(){
    $('.slider-principal').slick({
        autoplay: true, // Ativar autoplay
        autoplaySpeed: 6000, // Definir o intervalo de troca de imagens em milissegundos (6 segundos)
        infinite: true // Permitir rolagem infinita
    });
});